#include <stdio.h>
int main(){
    int a = 1;
    int b = 0;
    printf("a and b ----> %d\n", a && b);
    printf("a or b ----> %d\n", a || b);
    // printf("a not b ----> %d\n", a ! b);

    return 0;
}